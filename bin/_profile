#!/bin/bash

if [[ "$#" > 0 ]]; then
  ARBEE="$1"; shift
fi  

: ${ARBEE:="$(cd -P -- "$(dirname -- "${BASH_SOURCE}")/.." && pwd -P)"}

export RBENV_ROOT="$ARBEE"
PATH="$RBENV_ROOT/shims:$RBENV_ROOT/bin:$PATH"
hash -r
